import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'res.cloudinary.com', 'example.com'], // Add any other image domains you need
  },
}

export default nextConfig
